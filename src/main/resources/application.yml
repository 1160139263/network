# 数据库连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/network?serverTimezone=UTC&characterEncoding=utf-8
    username: root
    password: toor!@#123
#    sql文件
#    schema: sql/network.sql

# mybatis配置文件
mybatis:
  config-location: classpath:mybatis-config.xml

server:
  port: 8081

